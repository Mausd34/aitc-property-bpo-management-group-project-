import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import'./style.css';
const API='http://127.0.0.1:8000/api';const get=p=>fetch(API+p).then(r=>r.json());
function App(){const[d,setD]=useState(null),[orders,setO]=useState([]),[tab,setTab]=useState('Dashboard');const load=()=>Promise.all([get('/dashboard/'),get('/work-orders/')]).then(([a,b])=>{setD(a);setO(b)});useEffect(load,[]);return <div className="app"><aside><h2>AITC BPO</h2>{['Dashboard','Work Orders','Clients','Vendors','QA Review','Documents','Reports'].map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)}>{x}</button>)}</aside><main><header><div><h1>{tab}</h1><p>Property Preservation & BPO Management System</p></div><button onClick={load}>Refresh</button></header>{tab==='Dashboard'&&d&&<><div className="grid">{[['Clients',d.clients],['Properties',d.properties],['Work Orders',d.work_orders],['Vendors',d.vendors],['QA Reviews',d.qa_reviews],['Documents',d.documents]].map(x=><div className="stat"><small>{x[0]}</small><strong>{x[1]}</strong></div>)}</div><section><h2>Workflow</h2><div className="flow">Client → Work Order → Operations/Dispatch → Vendor → Completed Work → QA → Client</div></section></>}{tab==='Work Orders'&&<section><h2>Work Orders</h2><table><thead><tr><th>ID</th><th>Title</th><th>Client</th><th>Property</th><th>Priority</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr><td>{o.id}</td><td>{o.title}</td><td>{o.client}</td><td>{o.property}</td><td>{o.priority}</td><td>{o.status}</td></tr>)}</tbody></table></section>}{tab!=='Dashboard'&&tab!=='Work Orders'&&<section><h2>{tab}</h2><p>Use Django Admin and the REST API to manage this module. The backend already contains the database model and CRUD API for the requirement.</p></section>}</main></div>}createRoot(document.getElementById('root')).render(<App/>);
